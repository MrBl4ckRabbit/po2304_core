1.Лямбда может содержать 0 или более входных параметров
   ()->26;
   (String s)->{sout(s);}
   (int v1,int v2)->{return v1+v2;}
2. Тип параметров может быть явно объявлен либо выведен из значения параметров
   (String str)->{sout(str);}
   ==
   (s)->{sout(s);}
3.Если параметров нет вообще либо параметров несколько,то скобки обязательны
   (v1,v2)->return v1+v2;
   (int v1,int v2)-> return v1+ v2;
   ()->27;
4.Нет необходимости объявлять один параметр в скобках,но в этом случае нельзя явно указать тип
   v1->return 2*v1;
   (int v1)->return 2*v1
5.Нет неоходимости использовать {} и return если тело состоит из одного выражения
   ()->45;
   (int v1,int v2)-> v1+v2;
6.Если тело состоит больше,чем из одной строки ,то {} и return обязательны
   ()->{
       sout("Value=4")
       return 4;
   }
7.Если ключевое слово return отсутсвует то возвращаемый тип может быть void
   ()->sout("Hello world")
   ()->{
          sout("Value=4")
          return ;
      }
